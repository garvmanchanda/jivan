# NOTE: This is a template. DO NOT commit actual secrets to Git.
# Use kubectl create secret or external secret management (e.g., Sealed Secrets, External Secrets Operator)

apiVersion: v1
kind: Secret
metadata:
  name: jivan-secrets
  namespace: jivan
type: Opaque
stringData:
  DATABASE_URL: "postgresql://user:password@postgres-service:5432/jivan_db"
  REDIS_URL: "redis://redis-service:6379"
  REDIS_PASSWORD: ""
  AWS_ACCESS_KEY_ID: "your-aws-access-key"
  AWS_SECRET_ACCESS_KEY: "your-aws-secret-key"
  S3_BUCKET_NAME: "jivan-storage"
  OPENAI_API_KEY: "sk-your-openai-api-key"
  OPENAI_ORG_ID: ""
  FIREBASE_PROJECT_ID: "your-firebase-project-id"
  FIREBASE_PRIVATE_KEY: "your-firebase-private-key-base64"
  FIREBASE_CLIENT_EMAIL: "firebase-adminsdk@your-project.iam.gserviceaccount.com"
  ENCRYPTION_KEY: "your-32-byte-hex-encryption-key"
  ENCRYPTION_IV: "your-16-byte-hex-iv"
---
# To create secrets from file:
# kubectl create secret generic jivan-secrets \
#   --from-env-file=.env \
#   --namespace=jivan

